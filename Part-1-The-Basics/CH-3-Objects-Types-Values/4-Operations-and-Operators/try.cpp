#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    cout << "Enter an intger value number: ";
    int n;
    cin >> n;
    double m = (double)n;
    cout << "Square root of " << n << " is " << sqrt(m);
}